{
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "Run build sctipt",
            "type": "shell",
            "command": "${workspaceRoot}/build-app.sh",
            "args": [
                "${workspaceRoot}/filter-hawk",
                "${workspaceRoot}/filter-hawk.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}